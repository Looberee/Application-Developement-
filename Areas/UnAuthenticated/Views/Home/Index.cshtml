@{
    @if (User.Identity.IsAuthenticated && User.IsInRole("Customer"))
    {
        ViewData["Title"] = "Welcome Customer to Home Page";
    }
    else
    {
        ViewData["Title"] = "Home Page";     
    }
}